module.exports = {
  content: [
    './src/**/*.{js,jsx,ts,tsx}',
    'node_modules/flowbite-react/lib/esm/**/*.js',
  ],
  theme: {
    extend: {
      keyframes: {
        float: {
          '0%': { transform: 'translateY(0)' },
          '50%': { transform: 'translateY(-10px)' },
          '100%': { transform: 'translateY(0)' },
        },
        typewriter: {
          '0%': { width: '0' },
          '100%': { width: '100%' },
        },
        blink: {
          '50%': { borderColor: 'transparent' },
        },
      },
      animation: {
        float: 'float 3s ease-in-out infinite',
        typewriter: 'typewriter 4s steps(44) 1s 1 normal both, blink 750ms steps(44) infinite normal',
      },
    },
  },
  plugins: [
    require('flowbite/plugin')
  ],
}
